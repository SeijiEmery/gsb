module gsb.engine.event_thread;
import gsb.engine.thread_mgr;
import gsb.engine.engine;

class EventThreadManager {
    IEngine engine;

    // Call once from engine; runs the event loop.
    void runMainThread (ThreadManager.EThreadContext ctx) {

    }
}






















